# Dynamic-Sequence-Class
A class that is able to insert values within an array to store data. Can remove and attach new elements to the area by creating a new array and deleting old data from memory.
